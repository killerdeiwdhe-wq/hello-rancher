replicaCount: 1

image:
  repository: jun2281/hello-rancher
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080
  targetPort: 8080

ingress:
  enabled: true
  ingressClassName: traefik
  hosts:
    - host: ""
      paths:
        - path: /
          pathType: Prefix
  tls: []
